#include <stdio.h>
#include <stdlib.h>

typedef struct {
        int cols_num;
        int row;
        int col;
} Element;

int main(){
    int n,m;
    pritf("Podaj wymiary macierzy: ");
    scanf("%d %d", &n, &m);
    

    return 0;
}